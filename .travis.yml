sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t aurerelily/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true aurerelily/docker-react npm run test -- --coverage